<%= render InputField::Component.new(
  items: [
    InputField::Item.new(
      id: "login",
      name: "user[login]",
      label: t("users.label.email_or_name"),
      type: "text",
      icon_class: Icon::User,
      placeholder: t("users.label.email_or_name"),
      required: true,
    ),
    InputField::Item.new(
      id: "password",
      name: "user[password]",
      label: t("users.label.password"),
      type: "password",
      icon_class: Icon::Lock,
      placeholder: t("users.label.password"),
      required: true,
    ),
  ],
  url: url,
  classes: classes,
) do |component| %>
  <% component.with_button(
    label: t("users.login"),
    classes: "w-full mt-6",
  ) %>
<% end %>
