<%= render InputField::Component.new(
  items: [
    InputField::Item.new(
      id: "login",
      name: "user[login]",
      label: t("login.label.email_or_name"),
      type: "text",
      icon_class: Icon::User,
      placeholder: t("login.label.email_or_name"),
      required: true,
    ),
    InputField::Item.new(
      id: "password",
      name: "user[password]",
      label: t("login.label.password"),
      type: "password",
      icon_class: Icon::Lock,
      placeholder: t("login.label.password"),
      required: true,
    ),
  ],
  url: url,
  classes: classes,
) do |component| %>
  <% component.with_button(
    label: t("login.button.login"),
    classes: "w-full mt-6",
  ) %>
<% end %>
