<%= render Link::ButtonComponent.new(
  active: true,
  href: href,
  shape: Link::Button::ROUNDED,
  size: Link::Button::BASE,
  classes: classes,
) do %>
  <%= render Icon::List.new(classes: label? ? "me-2" : "") %>
  <% if label? %>
    <%= label %>
  <% end %>
<% end %>
