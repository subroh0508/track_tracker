<%= link_to href,
  class: classes,
  target: target? ? :_blank : nil,
  rel: target? ? "noopener noreferrer" : nil,
  data: data do
%>
  <% if icon? %>
    <i class="<%= icon %>"></i>
  <% end %>
  <%= content %>
<% end %>
