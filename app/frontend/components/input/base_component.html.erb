<input type="<%= type %>"
  id="<%= id %>"
  name="<%= name || id %>"
  class="<%= classes %>"
  placeholder="<%= placeholder || "" %>"
  <%= required? ? "required" : nil %>
  <%= disabled? ? "disabled" : nil %>
  value="<%= value || "" %>">
