<textarea id="<%= id %>"
  name="<%= name || id %>"
  class="<%= classes %>"
  placeholder="<%= placeholder || "" %>"
  rows="<%= rows %>"
  <%= required? ? "required" : nil %>
  <%= disabled? ? "disabled" : nil %>><%= value %></textarea>
