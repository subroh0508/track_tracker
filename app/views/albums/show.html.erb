<div class="flex max-w-5xl mx-auto">
  <%= render Albums::ContentComponent.new(
    album: @album,
    classes: "w-80 me-10",
  ) do %>
    <%= render Albums::Link::PlayerComponent.new(
      base_url: Albums::BASE_URL,
      album_id: @album[:id],
      label: "プレイヤーを開く",
    ) %>
  <% end %>
  <div class="grow">
    <%= render Albums::TracksComponent.new(
      @album[:tracks],
    ) %>
  </div>
</div>
