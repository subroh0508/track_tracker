<div class="flex max-w-5xl mx-auto">
  <%= render Albums::Form::ContentComponent.new(
    album: @album,
    classes: "w-80 me-10",
  ) do %>
  <% end %>
  <div class="grow">
    <%= render Albums::TracksComponent.new(
      @album[:tracks],
    ) %>
  </div>
</div>
